name: simple_hashtag
description: SimpleHashTag is a package that generates hashtags from strings.
version: 0.1.0
homepage: https://github.com/taiseidev/simple-hashtag

environment:
  sdk: ">=2.18.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
      ios:
      web:
